groups:
  - name: "openHAB Configuration"
    description: "Configure openHAB deployment"

portals:
  web_portal:
    protocols:
      - "http"
    host:
      - "$node_ip"
    ports:
      - "8080"
    path: "/"

questions:
  - variable: testVar
    label: "Test label"
    group: "openHAB Configuration"
    schema:
      type: boolean
      default: false
